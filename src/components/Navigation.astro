---
const currentPath = Astro.url.pathname;

// Handle both cases: with and without trailing slash
const isActiveHome = currentPath === '/' || currentPath === '/';
---

<nav>
    <a href="/" class={isActiveHome ? 'active' : ''}>PRPL SAGE</a>
</nav>
